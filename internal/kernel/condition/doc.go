// Package condition implements the Condition Engine: curve evaluation
// (interpolating delay/jitter/loss/bandwidth at a given point within a
// pass-shape profile) and the coverage scheduler (computing window
// open/close times and exposing lookahead before a transition).
package condition
