// Package profile defines the pass-shape profile schema (coverage window
// timing plus per-metric delay/jitter/loss/bandwidth curves) and the loader
// that reads and validates profile files (e.g. testdata/profiles/*.yaml).
package profile
